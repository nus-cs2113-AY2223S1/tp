/**
 * MainMenuState is an enumeration representing the state the main menu is at for navigating between sub menus.
 */
public enum MainMenuState {
    PATIENT,
    VISIT,
    PRESCRIPTION,
    INVALID,
    EXIT
}
