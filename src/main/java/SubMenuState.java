/**
 * SubMenuState is an enumeration representing the state the sub menu is at for navigating between sub menu and main
 * menu.
 */
public enum SubMenuState {
    IN_SUB_MENU,
    BACK_TO_MAIN,
    EXIT,
    HELP
}
